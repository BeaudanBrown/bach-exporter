config <- list(
  redcap_url = "https://redcap.example.org/api/",
  api_key = "REPLACE_WITH_ADMIN_TOKEN"
)

message("Admin snapshot refresh is not implemented yet.")
message(sprintf("Placeholder REDCap URL: %s", config$redcap_url))
